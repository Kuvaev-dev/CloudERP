@model IEnumerable<DatabaseAccess.tblAccountSubControl>

@{
    ViewBag.Title = "All Account Sub Controls";
}

<script src="~/Scripts/jquery-3.7.1.js"></script>

<div class="card card-default col-md-12">
    <div class="card-header">
        @Html.ActionLink("Add Account Sub Control", "Create", null, new { @style = "float:left;", @class = "btn btn-success" })
        @ViewBag.Message
        <input class="form-control col-md-3" style="float:right;" type="text" id="searchAccountSubControl" name="searchAccountSubControl" placeholder="Search..." />
    </div>
    <div class="card-body">
        <div class="table-responsive bootgrid">
            <table class="table table-striped table-bordered table-hover" id="dataTable">
                <thead>
                    <tr>
                        <th>
                            Head Account
                        </th>
                        <th>
                            Control Account
                        </th>
                        <th>
                            Sub Control Account
                        </th>
                        <th>
                            User
                        </th>
                        <th>
                            Actions
                        </th>
                    </tr>
                </thead>
                <tbody id="accountSubControlList">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.tblAccountHead.AccountHeadName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.tblAccountControl.AccountControlName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.AccountSubControlName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.tblUser.FullName)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.AccountSubControlID }, new { @class = "btn btn-primary" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        $("#searchAccountSubControl").on("keyup", function () {
            var value = $(this).val().toLowerCase();
            $("#accountSubControlList tr").filter(function () {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            })
        })
    })
</script>
