@model IEnumerable<DatabaseAccess.Models.SupplierReturnInvoiceModel>

@{
    ViewBag.Title = Resources.Purchase.ReturnPurchasesPaymentPending;
}

<script src="~/Scripts/jquery-3.7.1.js"></script>

<div class="card card-default col-md-12">
    <div class="card-header">
        <h3 class="text-danger">@ViewBag.ErrorMessage</h3>
        <h5 style="float:left;">@Session["Message"]</h5>
        <input class="form-control col-md-3" style="float:right;" type="text" id="searchPayment" name="searchPayment" placeholder="Search..." />
    </div>
    <div class="card-body">
        <div class="table-responsive bootgrid">
            <table class="table table-striped table-bordered table-hover" id="dataTable">
                <thead>
                    <tr>
                        <th>
                            @Resources.Supplier.SupplierName
                        </th>
                        <th>
                            @Resources.Miscellaneous.InvoiceNo
                        </th>
                        <th>
                            @Resources.Purchase.InvoiceDate
                        </th>
                        <th>
                            @Resources.Purchase.ReturnTotalAmount
                        </th>
                        <th>
                            @Resources.Purchase.ReturnPaymentAmount
                        </th>
                        <th>
                            @Resources.Purchase.RemainingBalance
                        </th>
                    </tr>
                </thead>
                <tbody id="paymentList">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.ActionLink(item.SupplierName, "SupplierDetails", "Supplier", new { id = item.SupplierID }, null)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.InvoiceNo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.InvoiceDate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ReturnTotalAmount)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ReturnPaymentAmount)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.RemainingBalance)
                            </td>
                            <td>
                                @{
                                    if (item.ReturnTotalAmount - 1 > item.ReturnPaymentAmount)
                                    {
                                        Html.ActionLink(Resources.Purchase.ReturnPayment, "ReturnAmount", new { id = item.SupplierReturnInvoiceID }, new { @class = "btn btn-primary" });
                                    }
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        $('#dataTable').DataTable({
            "columnDefs": [{ "orderable": false, "targets": 3 }],
            "lengthMenu": [[5, 10, 25, 50, 75, 100, 150, 200, -1], [5, 10, 25, 50, 75, 100, 150, 200, "All"]],
        });
    });
</script>