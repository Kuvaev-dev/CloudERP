@model IEnumerable<Domain.Models.FinancialModels.PurchasePaymentModel>

@{
    ViewBag.Title = Localization.CloudERP.Purchase.AllReturnPurchasesPendingReturnPayment.ToString();
}

<!-- Tables Sorting-->
<link href="https://cdn.datatables.net/1.11.6/css/jquery.dataTables.min.css" rel="stylesheet" />
<script src="~/Scripts/jQuery/jquery-3.7.1.js"></script>
<script src="https://cdn.datatables.net/1.11.6/js/jquery.dataTables.min.js"></script>

<style>
    #dataTable_filter input[type="search"] {
        width: 15rem;
    }
</style>

<div class="card card-default col-md-12">
    <div class="card-body">
        <div class="table-responsive bootgrid">
            <table class="table table-striped table-bordered table-hover" id="dataTable">
                <thead>
                    <tr>
                        <th>
                            @Localization.CloudERP.Purchase.Supplier.ToString()
                        </th>
                        <th>
                            @Localization.CloudERP.Miscellaneous.ContactNo.ToString()
                        </th>
                        <th>
                            @Localization.CloudERP.Miscellaneous.InvoiceNo.ToString()
                        </th>
                        <th>
                            @Localization.CloudERP.Miscellaneous.Date.ToString()
                        </th>
                        <th>
                            @Localization.CloudERP.Purchase.TotalAmount.ToString()
                        </th>
                        <th>
                            @Localization.CloudERP.Purchase.ReturnProductsTotalAmount.ToString()
                        </th>
                        <th>
                            @Localization.CloudERP.Purchase.AfterReturnTotalAmount.ToString()
                        </th>
                        <th>
                            @Localization.CloudERP.Purchase.PaidAmount.ToString()
                        </th>
                        <th>
                            @Localization.CloudERP.Purchase.PaymentAmount.ToString()
                        </th>
                        <th>
                            @Localization.CloudERP.Purchase.RemainingBalance.ToString()
                        </th>
                        <th>
                            @Localization.CloudERP.Common.Actions.ToString()
                        </th>
                    </tr>
                </thead>
                <tbody id="paymentList">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.SupplierName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.SupplierContactNo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.InvoiceNo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.InvoiceDate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TotalAmount)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ReturnProductAmount)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.AfterReturnTotalAmount)
                            </td>
                            <td>
                                @{
                                    if (item.PaymentAmount == 0 && item.AfterReturnTotalAmount > 0)
                                    {
                                        @Html.ActionLink(Localization.CloudERP.Purchase.SetPayment.ToString(), "PaidAmount", "PurchasePayment", new { id = item.SupplierInvoiceID }, new { @class = "text-success" })
                                    }
                                    else
                                    {
                                        @Html.DisplayFor(modelItem => item.PaymentAmount)
                                    }
                                }
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ReturnPaymentAmount)
                            </td>
                            <td>
                                @Html.ActionLink(item.RemainingBalance.ToString("F2"), "ReturnPurchasePendingAmount", "PurchasePaymentReturn", new { @id = item.SupplierInvoiceID }, new { @class = "btn btn-outline-danger btn-lg" })
                            </td>
                            <td class="btn-group d-flex flex-column">
                                @{
                                    if (item.RemainingBalance < item.TotalAmount && item.PaymentAmount != 0)
                                    {
                                        @Html.ActionLink(Localization.CloudERP.Purchase.PayHistory.ToString(), "PaidHistory", "PurchasePayment", new { id = item.SupplierInvoiceID }, new { @class = "btn btn-primary mb-2" });
                                    }
                                    else
                                    {
                                        <em class="icon-book-open">
                                            <label class="text-danger"><b>@Localization.CloudERP.Purchase.NoHistory.ToString()</b></label>
                                        </em>
                                    }
                                }
                                @Html.ActionLink(Localization.CloudERP.Purchase.ViewDetails.ToString(), "PurchaseItemDetail", "PurchasePayment", new { id = item.SupplierInvoiceID }, new { @class = "btn btn-info mt-2 mb-2" });
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        $('#dataTable').DataTable({
            "lengthMenu": [[5, 10, 25, 50, 75, 100, 150, 200, -1], [5, 10, 25, 50, 75, 100, 150, 200, "All"]],
        });
    });
</script>