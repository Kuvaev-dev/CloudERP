@model Domain.Models.AnalyticsModel
@{
    ViewBag.Title = "System Analytics";
}

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-primary text-white">Users Activity</div>
                <div class="card-body">
                    <canvas id="usersChart" width="400" height="200"></canvas>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-info text-white">Stock</div>
                <div class="card-body">
                    <canvas id="stockChart" width="400" height="200"></canvas>
                </div>
            </div>
        </div>
    </div>

    <div class="row mb-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-success text-white">Support</div>
                <div class="card-body">
                    <canvas id="supportChart" width="400" height="200"></canvas>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    const chartData = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(ViewBag.ChartData));

    // Users
    const usersCtx = document.getElementById('usersChart').getContext('2d');
    new Chart(usersCtx, {
        type: 'polarArea',
        options: {
            responsive: true
        },
        data: {
            labels: ['All', 'New in a Month', 'New'],
            datasets: [{
                label: 'Users',
                data: [chartData.Employees.Total, chartData.Employees.NewInAMonth, chartData.Employees.NewInAYear],
                backgroundColor: ['#007bff', '#28a745', '#ffc107']
            }]
        }
    });

    // Stock
    const stockCtx = document.getElementById('stockChart').getContext('2d');
    new Chart(stockCtx, {
        type: 'polarArea',
        options: {
            responsive: true
        },
        data: {
            labels: ['All', 'In Stock'],
            datasets: [{
                label: 'Stock',
                data: [chartData.Stock.TotalItems, chartData.Stock.Available, chartData.Stock.Expired],
                backgroundColor: ['#17a2b8', '#6c757d']
            }]
        }
    });

    // Support
    const supportCtx = document.getElementById('supportChart').getContext('2d');
    new Chart(supportCtx, {
        type: 'polarArea',
        options: {
            responsive: true
        },
        data: {
            labels: ['All', 'Resolved', 'Pending'],
            datasets: [{
                label: 'Support',
                data: [chartData.Support.TotalTickets, chartData.Support.Resolved, chartData.Support.Pending],
                backgroundColor: ['#6c757d', '#28a745', '#ffc107']
            }]
        }
    });
</script>