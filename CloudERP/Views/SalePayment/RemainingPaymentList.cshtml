@model IEnumerable<DatabaseAccess.Models.SalePaymentModel>

@{
    ViewBag.Title = Resources.SalePayment.SalesPaymentPendingList.ToString();
}

<!-- Tables Sorting-->
<link href="https://cdn.datatables.net/1.11.6/css/jquery.dataTables.min.css" rel="stylesheet" />
<script src="~/Scripts/jquery-3.7.1.js"></script>
<script src="https://cdn.datatables.net/1.11.6/js/jquery.dataTables.min.js"></script>

<style>
    #dataTable_filter input[type="search"] {
        width: 15rem;
    }
</style>

<div class="card card-default col-md-12">
    <div class="card-header">
        <h5 style="float:left;">@Session["Message"]</h5>
    </div>
    <div class="card-body">
        <div class="table-responsive bootgrid">
            <table class="table table-striped table-bordered table-hover" id="dataTable">
                <thead>
                    <tr>
                        <th>
                            @Html.Raw(HttpUtility.HtmlDecode(Resources.SalePayment.Customer))
                        </th>
                        <th>
                            @Html.Raw(HttpUtility.HtmlDecode(Resources.SalePayment.ContactNo))
                        </th>
                        <th>
                            @Html.Raw(HttpUtility.HtmlDecode(Resources.SalePayment.InvoiceNo))
                        </th>
                        <th>
                            @Html.Raw(HttpUtility.HtmlDecode(Resources.SalePayment.Date))
                        </th>
                        <th>
                            @Html.Raw(HttpUtility.HtmlDecode(Resources.SalePayment.TotalAmount))
                        </th>
                        <th>
                            @Html.Raw(HttpUtility.HtmlDecode(Resources.SalePayment.PaidAmount))
                        </th>
                        <th>
                            @Html.Raw(HttpUtility.HtmlDecode(Resources.SalePayment.RemainingBalance))
                        </th>
                    </tr>
                </thead>
                <tbody id="paymentList">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.CustomerName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CustomerContactNo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.InvoiceNo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.InvoiceDate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TotalAmount)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PaymentAmount)
                            </td>
                            <td>
                                <label>@(item.TotalAmount - item.PaymentAmount)</label>
                            </td>
                            <td class="btn-group">
                                @{
                                    if (item.TotalAmount - 1 > item.PaymentAmount)
                                    {
                                        Html.ActionLink(Resources.SalePayment.PayAmount.ToString(), "PaidAmount", new { id = item.CustomerInvoiceID }, new { @class = "btn btn-primary" });
                                    }
                                    if (item.PaymentAmount > 0)
                                    {
                                        Html.ActionLink(Resources.SalePayment.PayHistory.ToString(), "PaidHistory", new { id = item.CustomerInvoiceID }, new { @class = "btn btn-info" });
                                    }
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        $('#dataTable').DataTable({
            "lengthMenu": [[5, 10, 25, 50, 75, 100, 150, 200, -1], [5, 10, 25, 50, 75, 100, 150, 200, "All"]],
        });
    });
</script>

