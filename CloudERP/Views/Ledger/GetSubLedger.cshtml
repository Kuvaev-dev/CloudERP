@model IEnumerable<Domain.Models.FinancialModels.AccountLedgerModel>
@{
    ViewBag.Title = Localization.CloudERP.Miscellaneous.SubBranchAccountsLedger.ToString();
}

<script src="~/Scripts/jQuery/jquery-3.7.1.js"></script>

@using (Html.BeginForm("GetSubLedger", "Ledger", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    <div class="card">
        <div class="card card-float">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-4">
                        <label>@Localization.CloudERP.Miscellaneous.SelectFinancialYear.ToString()</label>
                        @Html.DropDownList("id", (SelectList)ViewBag.FinancialYears, Localization.CloudERP.Miscellaneous.SelectFinancialYear.ToString(), new { @class = "form-control", required = "required" })
                        <input type="submit" value="@Localization.CloudERP.Miscellaneous.RetrieveLedger.ToString()" class="btn btn-success form-control mt-2">
                    </div>
                    <div class="col-md-4">
                        <h3 class="text-danger">@ViewBag.Message</h3>
                        <h3 class="text-danger">@ViewBag.ErrorMessage</h3>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

<div class="card">
    <div class="card-header">
        <div class="card-title">@Localization.CloudERP.Miscellaneous.Export.ToString()</div>
        <div class="text-sm">
            @Localization.CloudERP.Miscellaneous.AccountsLedger.ToString()
        </div>
    </div>
    <div class="card-body">
        <table class="table my-4 w-100" id="datatable2">
            <thead>
                <tr>
                    <th style="display:none;">
                        %
                    </th>
                    <th>
                        @Localization.CloudERP.Miscellaneous.Account.ToString()
                    </th>
                    <th>
                        @Localization.CloudERP.Miscellaneous.Date.ToString()
                    </th>
                    <th>
                        @Localization.CloudERP.Miscellaneous.Description.ToString()
                    </th>
                    <th>
                        @Localization.CloudERP.Miscellaneous.Debit.ToString()
                    </th>
                    <th>
                        @Localization.CloudERP.Miscellaneous.Credit.ToString()
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    if (item.Date == "Date")
                    {
                        <tr style="background-color:lightgray">
                            <td style="display:none;">
                                @Html.DisplayFor(modelItem => item.SNo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Account)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Date)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Description)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Debit)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Credit)
                            </td>
                        </tr>
                    }
                    else
                    {
                        <tr>
                            <td style="display:none;">
                                @Html.DisplayFor(modelItem => item.SNo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Account)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Date)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Description)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Debit)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Credit)
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>

<script type="text/javascript">
    $(document).ready(function () {
        $.ajax({
            type: "GET",
            url: "/TrialBalance/GetFinancialYear",
            data: {},
            dataType: 'Json',
            success: function (response) {
                $('#id').append("<option value='0'>Select Financial Year</option>")
                for (var i = 0; i < respone.data.length; i++) {
                    $('#id').append("<option value=" + response.data[i].FinancialYearID + ">" + response.data[i].FinancialYear + "</option>")
                }
            }
        })
    })
</script>
